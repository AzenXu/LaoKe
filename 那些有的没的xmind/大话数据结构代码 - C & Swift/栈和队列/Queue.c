/*
 ADT 队列(Queue)
 Data
	同线性表。元素具有相同的数据类型，相邻元素具有直接的前驱后继关系
 Operation
	InitQueue(*Q) 初始化，建立一个空队列
	DestroyQueue(*Q)
	ClearQueue(*Q)
	QueueEmpty(Q)
	GetHead(Q, *e)
	EnQueue(*Q, e)
	DeQueue(*Q, *e)
	QueueLength(Q)
 endADT
 
 */
